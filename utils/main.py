from utils.addition import add
from utils.subtraction import sub
from utils.multiplication import mult
from utils.division import div


adding = add(3,5)
print(f"addition : {adding}")

subtract = sub(4,2)
print(f"subtraction : {subtract}")

multiplicate = mult(4,5)
print(f"multiplication : {multiplicate}")

divide = div(10,2)
print(f"division : {divide}")
